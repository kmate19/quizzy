{
    "name": "quizzy",
    "private": true,
    "scripts": {
        "dev": "turbo watch dev drizzle:studio",
        "build": "turbo build",
        "start": "turbo start",
        "test": "turbo test",
        "prod_test": "docker compose -f compose_prod_test.yaml up -d --force-recreate --build",
        "db_dev": "docker compose -f compose_dev.yaml up -d --force-recreate --build",
        "lint": "turbo lint",
        "format": "prettier --write \"**/*.{ts,tsx,md}\""
    },
    "devDependencies": {
        "prettier": "^3.5.1",
        "turbo": "^2.4.2",
        "typescript": "^5.7.3"
    },
    "packageManager": "pnpm@9.13.2",
    "engines": {
        "node": ">=18"
    }
}
